apiVersion: serving.knative.dev/v1alpha1
kind: Service
metadata:
  name: __NAME__
  namespace: default
spec:
  runLatest:
    configuration:
      revisionTemplate:
        spec:
          container:
            image: __IMAGE__
